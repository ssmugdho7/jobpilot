services:
  - type: web
    name: jobpilot
    runtime: image
    image:
      url: ""  # uses Dockerfile from repo root
    dockerfilePath: Dockerfile
    envVars:
      - key: PORT
        value: 8080
      - key: GEMINI_MODEL
        value: gemini-3.5-flash-preview
